import React from 'react';

const Exponentially = () => {
    return (
        <div className='border border-green-600 w-screen h-96'>
            Exponentially
        </div>
    );
};

export default Exponentially;